import React from 'react';
import './App.scss';
import { Init } from './modules/core';

export const App: React.FC = () => {
   return (
      <div className='Init'>
         <Init />
      </div>
   );
};
